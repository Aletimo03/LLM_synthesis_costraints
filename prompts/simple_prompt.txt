Generate valid SDC constraints for this verilog design:

module simple(
    input clk,
    input a,
    output reg y
);
    always @(posedge clk) begin
        y <= a;
    end
endmodule

Rules:
- Use only these ports: clk, a, y
- Output only raw SDC commands
- Do not explain anything
- Do not include markdown
- Do not include comments
- Do not include reasoning